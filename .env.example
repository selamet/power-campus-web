# Base URL of the Power Akademi REST API
VITE_API_BASE_URL=https://api.powerakademi.com

# Request timeout in milliseconds
VITE_API_TIMEOUT=15000

# When "true", the data layer resolves bundled mock data instead of calling the API.
# Useful until the backend is available.
VITE_USE_MOCKS=true
